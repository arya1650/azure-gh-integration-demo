trigger:
  - main

pool:
  name: Default


steps:

- script: |
    echo "Starting fake Java Build"
    bash build.sh
    echo "Listing contents of build folder"
    ls -l build/

  displayName: 'Build fake .jar'
