trigger:
  - main

pool:
  name: Default

steps:
- script: echo "GitHub integration pipeline triggered successfully!"
  displayName: 'Pipeline Trigger Check'

- script: bash test.sh
  displayName: 'Run Build and Test'
