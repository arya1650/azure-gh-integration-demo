trigger:
  - main

pool:
  vmImage: ubuntu-latest


steps:

- script: |
    echo "Starting fake Java Build"
    mkdir -p build
    echo "This simulates a .jar file" > build/fake-app.jar
    echo "Listing contents of build folder"
    ls build/

  displayName: 'Build fake .jar'
